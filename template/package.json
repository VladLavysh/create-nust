{
  "name": "__PROJECT_NAME__",
  "version": "__PROJECT_VERSION__",
  "description": "description",
  "author": "author",
  "license": "ISC",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @nust/shared build && pnpm --parallel --filter @nust/api --filter @nust/web run dev",
    "build": "pnpm --filter @nust/shared build && pnpm --parallel --filter @nust/api --filter @nust/web run build",
    "lint": "pnpm --parallel -r lint",
    "docker:db": "docker compose up postgres",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
